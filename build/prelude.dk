builtins.range=(start, end, step)->[
    iterator=self->{
        [i=start, next=self->{
            fin=self.i>end
            result=[key=self.i, value=self.i, finished=fin]
            self.i+=step
            result
        }]
    }
]