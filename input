arr=[3, 5, 6]

sum=(arr, index)->{
    if(arr[index]) arr[index]+sum(arr, index+1)
}
count=arr->{
    count_recursive=(arr, index)->{
        if(arr[index]) 1+count_recursive(arr, index+1) else 0
    }
    count_recursive(arr, 0)
}
count(arr)