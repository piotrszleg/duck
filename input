range=(start, end, step)->[
    iterator=self->{
        [i=start, call=self->{
            fin=self.i>end
            result=[key=self.i, value=self.i, finished=fin]
            self.i+=step
            result
        }]
    }
]
range(0, 10, 2) `##` (k, v)->print(string(k)+"=>"+string(v))